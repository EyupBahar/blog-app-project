import React from "react";
import PostContainer from "../components/PostContainer";

const Dashboard = () => {
  return (
    <div>
      <PostContainer />
    </div>
  );
};

export default Dashboard;
